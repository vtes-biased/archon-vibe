---
# Root's ~/.ansible/tmp must precede the common role's first root-context
# copy/get_url (unattended_upgrades, python).
- name: Ensure ansible tmp dir exists under root home
  ansible.builtin.file:
    path: /root/.ansible/tmp
    state: directory
    owner: root
    group: root
    mode: "0700"

- name: Ensure archon group exists
  ansible.builtin.group:
    name: "{{ archon_group }}"
    state: present
    system: true

- name: Ensure archon system user exists
  ansible.builtin.user:
    name: "{{ archon_user }}"
    group: "{{ archon_group }}"
    state: present
    shell: /usr/sbin/nologin
    system: true
    home: "{{ opt_root }}"
    create_home: false

- name: Ensure /opt/archon exists
  ansible.builtin.file:
    path: "{{ opt_root }}"
    state: directory
    owner: "{{ archon_user }}"
    group: "{{ archon_group }}"
    mode: "0755"

- name: Ensure ansible tmp dir exists under archon home
  ansible.builtin.file:
    path: "{{ opt_root }}/.ansible/tmp"
    state: directory
    owner: "{{ archon_user }}"
    group: "{{ archon_group }}"
    mode: "0700"

- name: Ensure /etc/archon exists (env files)
  ansible.builtin.file:
    path: "{{ env_dir }}"
    state: directory
    owner: root
    group: "{{ archon_group }}"
    mode: "0750"

# /var/lib/archon and its snapshots dir belong to tasks/snapshot_dirs.yml,
# imported by the deploy playbook: nginx-facing perms must ride every deploy.
