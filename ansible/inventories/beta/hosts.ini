[archon]
; Beta runs on the server-setup-managed frankfurt host as the `deploy` user.
; CI overrides ansible_host via the DEPLOY_HOST variable; for a local
; `just deploy-beta` this inventory value is used directly.
frankfurt ansible_host=57.129.110.107 ansible_user=deploy

[archon:vars]
archon_env=beta
