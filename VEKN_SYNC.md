# VEKN Sync

Integration with the VEKN API at vekn.net. Two directions: inbound pull (members + tournaments) and outbound push (tournament events + results + members).

## Feature Flags

| Flag | Scope | Effect |
|------|-------|--------|
| `VEKN_PUSH=true` | backend env | enables all outbound push operations |
| `VITE_VEKN_PUSH=true` | frontend env | restricts `max_rounds` UI to 2–4; shows VEKN link badge and amber pending-sync badges |
| `VEKN_SYNC_ENABLED=true` | backend env | enables periodic inbound member + tournament sync |
| `VEKN_SYNC_INTERVAL_HOURS` | backend env | inbound sync period (default 6h) |

**Required credentials** (all directions): `VEKN_API_BASE_URL`, `VEKN_API_USERNAME`, `VEKN_API_PASSWORD`

## Outbound Push (`vekn_push.py`)

### Real-time (fire-and-forget)

All real-time pushes are `asyncio.create_task` — they never block user requests. The object is saved with the flag unset before the task runs; failures are log-only and the hourly batch retries via the unset flag.

| Trigger | Function | Flag set on success |
|---------|----------|---------------------|
| Tournament create | `push_tournament_event()` | `external_ids["vekn"]` |
| Tournament finish | `push_tournament_results()` | `vekn_pushed_at` |
| Member sponsor / create | `push_member_background()` | `vekn_synced=true` |

Each function SSE-broadcasts the updated object after saving so clients reflect changes without reconnecting.

### Batch retry (hourly)

`batch_push()` runs hourly and catches all missed real-time pushes:
- Members: `vekn_synced=false`
- Tournament events: `external_ids.vekn` absent
- Tournament results: `vekn_pushed_at IS NULL` AND `rounds` array non-empty

The `rounds` guard excludes imported/ETL-migrated tournaments: their results came FROM vekn.net and must never be re-uploaded (the importer folds finals into standings; archondata assumes prelim-only, so a re-push would send wrong numbers).

### archondata format

```
{nrounds}¤{rank}§{first}§{last}§{city}§{vekn}§{gw}§{vp}§{vpf}§{tp}§{toss}§{rtp}§...
```

Generated by `generate_archondata()`. GW is prelim-only (finals GW removed for winner).

**Format → VEKN event type** (`FORMAT_RANK_TO_VEKN_TYPE`):

| Format | Rank | VEKN type ID |
|--------|------|--------------|
| Standard | Basic | 2 |
| Standard | NC | 8 |
| Standard | CC | 6 |
| Limited | Basic | 3 |
| V5 | Basic | 16 |

### Push constraints

- `max_rounds` is immutable once pushed to VEKN (enforced backend + frontend)
- VEKN requires: name 3–120 chars, rounds 2–4, organizer must have `vekn_id`
- All players must have `vekn_id` before results can be pushed
- Organizer impersonated via `Vekn-Id` header on `create_event`

### Frontend pending-sync badges (VITE_VEKN_PUSH only)

Amber badge shown to organizers when:
- Tournament header: state=Finished and `vekn_pushed_at` is null
- VeknManagement / ProfileView: user `vekn_synced=false`

i18n keys: `vekn_sync_pending_results`, `vekn_sync_pending_member`, `vekn_sync_pending_hint`

## Inbound Pull (`vekn_sync.py`, `vekn_tournament_sync.py`)

### Member sync

`VEKNSyncService` pulls the full VEKN roster and reconciles with local users:
- Creates User objects for unknown VEKN IDs
- Updates names, countries, roles for existing members
- Infers `coopted_by` relationships
- Non-destructive: fields in `local_modifications` are never overwritten

Tracking fields on User: `vekn_synced` (bool), `vekn_synced_at` (timestamp), `local_modifications` (set of field names).

### Tournament sync

`sync_all_tournaments()` imports historical tournaments:
- Creates Tournament objects for past events
- Seeds venue autocomplete data
- Stamps `vekn_pushed_at=now` on finished imports so batch_push never re-uploads them

### Error handling

Each sync phase (member, tournament, TWDA) is wrapped independently. An exception or timeout logs an error and skips that phase for the current cycle without aborting the others.

### Triggers

- Scheduled: every `VEKN_SYNC_INTERVAL_HOURS` hours
- Manual: `POST /admin/sync-vekn` (members + tournaments), `POST /admin/sync-vekn-tournaments` (tournaments only)

## Key Files

- `backend/src/vekn_push.py` — outbound push functions + batch_push
- `backend/src/vekn_api.py` — VEKN API client (auth, HTTP)
- `backend/src/vekn_sync.py` — inbound member sync service
- `backend/src/vekn_tournament_sync.py` — inbound tournament import
