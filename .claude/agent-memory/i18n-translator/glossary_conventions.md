---
name: glossary_conventions
description: Coined phrases, word-order rules, and specific precedents (VEKN ID order, sponsorship wording, write-once phrase, etc.) not captured in the core glossary table
metadata:
  type: project
---

## Coined phrases & ordering precedents
- Clock stop has two keys: `timer_clock_stop` (imperative/button) vs `timer_policy_clock_stop` (noun/policy) — es/pt/it differ per the [[glossary]] "clock stop" row; fr is identical for both.
- "VEKN ID" word order (checked 2026-07-12 by grep frequency across each file, not a single example): fr/pt/it keep English order "VEKN ID" (dominant, e.g. `vekn_no_id` "Pas de VEKN ID"/"Sem VEKN ID"/"Nessun VEKN ID"); es reverses to "ID VEKN" (`vekn_no_id`="Sin ID VEKN"). Don't swap these per-language orders. **Refinement (2026-07-13)**: this is a *file-wide* default, but some local UI sections have their own established sub-convention that overrides it — check sibling keys in the same section before trusting the global count. Confirmed: the `tournament_vekn_id_required_to_register`/`add_player_no_vekn_id`/`vekn_sponsor_to_register_message` cluster uses fr "identifiant VEKN" (translated, not "VEKN ID") and es "ID de VEKN" (with "de", not the file's usual bare "ID VEKN") consistently across all 3 keys — while the `profile_claim_vekn_*`/`profile_abandon_vekn_*` cluster uses fr/es/pt/it "VEKN ID"/"ID VEKN" per the normal file-wide rule. it's `tournament_vekn_id_required_to_register` cluster likewise prefers "ID VEKN" (reversed) even though it's file-wide default is "VEKN ID".
- "desk" (as in "the organizer at the registration desk") must NOT be translated as table/mesa/tavolo — those words are already heavily overloaded in this codebase for the VTES *game table* (fr `table`, es/pt `mesa`, it `tavolo`, 80–107 occurrences each). Use `bureau`(fr)/`mostrador`(es)/`balcão`(pt)/`banco`(it) instead — see [[glossary]] row "registration/check-in desk". Coined 2026-07-13 for `vekn_guidance_new_member` and the `pg_login_reset` sponsor sentence; none of these words collided with existing strings at the time.
- Sponsorship guidance was reworded 2026-07-13 to stop pointing specifically at "the coordinator of your country" (fr coordinateur/es coordinador/pt coordenador/it coordinatore) and instead point at the generic "official" (VEKN official — NC, Prince, or tournament organizer are all examples) — reflects that sponsorship isn't NC-exclusive. When touching sponsor-adjacent copy (`profile_sponsorship_banner`, `community_sponsor_cta`, `pg_login_reset`, `vekn_guidance_*`), prefer "official"/officiel/oficial/ufficiale over "coordinator", unless the string is specifically and only about the National Coordinator role.
- `vekn_sponsor_ineligible` (2026-07-13) dropped the cross-country sponsoring restriction entirely — any VEKN official can now sponsor any new member regardless of country — same trend as the row above. New canonical text: en "Only VEKN officials (IC, NC, or Prince) can sponsor new members. Ask an official to add them." → fr "Seuls les officiels VEKN (IC, NC ou Prince) peuvent parrainer de nouveaux membres. Demandez à un officiel de les ajouter." / es "Solo los oficiales de VEKN (IC, NC o Prince) pueden patrocinar nuevos miembros. Pida a un oficial que los añada." / it "Solo gli ufficiali VEKN (IC, NC o Prince) possono sponsorizzare nuovi membri. Chiedi a un ufficiale di aggiungerli." / pt "Apenas oficiais da VEKN (IC, NC ou Prince) podem patrocinar novos membros. Peça a um oficial para adicioná-los." Trio order "IC, NC, or Prince" here matches en directly (unlike the `og_offline` case below where en's order had to be overridden) — this key's *pre-existing* text already ordered IC-then-NC/Prince, so no conflict arose. The English source switched from "sponsor" to bare "add" for the second sentence (avoiding repetition) — translated literally as the **"Add Player" button verb** (see [[glossary]] "add" row), NOT the register-family verb, even though this string lives in the add-player flow. Rule of thumb: when en text switches from "register/sponsor" to bare "add" mid-string, mirror that switch to the literal add-verb rather than normalizing back to the register-family verb.
- "reported/pushed to VEKN" (the organization, not the vekn.net site) preposition, per `og_faq_q_vekn_push`: fr "sur VEKN" (or "à VEKN" for a "transmis à" verb), es "a VEKN" (no article), pt "para a VEKN" (WITH article — contraction, unlike es), it "al VEKN" (WITH article, a+il contraction). When the target is the *site* `vekn.net` instead, no article is needed in any of the four (it's a domain name): fr "transmis à vekn.net", es "reportados a vekn.net", pt "reportados para vekn.net", it "riportati a vekn.net". Don't conflate the two — "VEKN" (org) takes an article in pt/it but not es; "vekn.net" (site) never takes one.
- VEKN report "write-once" concept (results submitted once, never auto-updated after edits/reopening — only a VEKN admin can fix manually): no prior precedent existed before `finish_confirm_vekn`/`reopen_confirm_vekn_warn`/`vekn_out_of_sync_hint` (added 2026-07-12) — coined one canonical phrase per locale and reused it verbatim across all three keys for internal consistency: fr "le rapport n'est envoyé qu'une seule fois", es "el informe se envía una sola vez", pt "o relatório é enviado apenas uma vez", it "il rapporto viene inviato una sola volta". Reuse these exact phrases for any future VEKN-write-once string rather than re-coining.
- Dynamic-count sentences with a verb (e.g. "{count} player(s) will be ranked and rated") have no established precedent for verb number agreement (flat JSON, no plural system). Convention adopted 2026-07-12: keep the noun-level ambiguity marker per locale (fr `joueur(s)`, es `jugador/es`, pt `jogador/es`, it `giocatore/i`) but default the verb to **plural** form unconditionally (e.g. es "no tienen", not "no tiene/tienen") — a pragmatic compromise, not a confirmed team decision; flag if a stricter approach is ever requested.
- "Reporting/reported" verb choice differs by sense: "reporting VP scores at your table" (`pg_checkin_details` heading "Reporting Results") uses fr "Saisir"(entering)/es "Registro"/pt "Registrando"/it "Riportare"(same word, different sense) — don't reuse that heading's fr/es/pt verb for "reported to VEKN/vekn.net" (external system submission), which instead uses fr "transmis"/es "reportado"/pt "reportado"/it "riportato" (it happens to reuse the same word for both senses; fr/es/pt do not).
- "Decklist becomes public" (`finish_confirm_decks_*`, added 2026-07-12): coined by analogy with the existing "become visible to all members" pattern (`og_faq_a_decklists_mode`: fr "deviennent visibles"/es "se vuelven visibles"/pt "ficam visíveis"/it "diventano visibili") — no prior precedent for "public" specifically. Used: fr "deviennent publiques", es "se hacen públicas", pt "se tornam públicas", it "diventano pubbliche". "decklist" is feminine in all four (la/la/a/la), so agreement is `publique(s)`/`pública(s)`/`pública(s)`/`pubblica/pubbliche`.
- Keep in English (all langs): VEKN, VP, GW, TP, TWDA, VDB, Archon, Discord, Markdown, IndexedDB, QR, Standard, Grand Prix, Inner Circle (IC), NC, Prince, passkey, multideck.
- **Correction (2026-07-13)**: Constructed/Limited are NOT uniformly kept in English — es/pt/it do, but **fr translates them** ("Construit"/"Limité", confirmed in tab labels `rankings_cat_*` and in `pg_standings` prose). Always check the locale's own tab-label keys before assuming these stay English; see [[glossary]] row "Constructed / Limited" and footnote ‡‡.
- `rankings_cat_{constructed,constructed_online,limited,limited_online}_desc` (added 2026-07-13, rankings-page tab captions): the exact phrasing for "rating points"/"best N tournaments over 18 months" was NOT coined fresh — it was pulled verbatim from the pre-existing `pg_standings` help-guide section ("Rating e Classifiche" it / "Ratings y clasificaciones" es / "Classements et ratings" fr / "Classificações e Rankings" pt) which explains the identical rating formula. Deliberately translated en "results" as "tournois/torneos/torneios/tornei" (tournaments) rather than literally, to match that established phrase rather than coin a synonym — see [[glossary]] "best N tournaments" row. **When a new string describes a concept already explained in a help-guide (`pg_*`/`og_*`) body, grep that body first — it's often the highest-authority precedent, higher than reconstructing from the glossary table.**
- NOTE/TIP callout titles per locale: fr NOTE→NOTE, TIP→CONSEIL; es NOTE→NOTA, TIP→CONSEJO; it NOTE→NOTA, TIP→CONSIGLIO (pg_ context) / SUGGERIMENTO (first-install); pt NOTE→NOTA, TIP→DICA. AVERTISSEMENT(fr)/ADVERTENCIA(es)/ATTENZIONE(it) for warnings.
- Anchor link convention: fr/es/pt keep English slugs unchanged; it localizes slugs to match the translated heading (e.g. `#self-organized-rounds` → `#ronde-auto-organizzate`, `#seating-rules-reference` → `#riferimento-regole-di-seduta`).
- `og_force_takeover` was split in en.json: the tail (Archon Import + Leagues + Seating Rules Reference + FAQ heading) moved to new key `og_reference`. When any locale's `og_force_takeover` still contains that tail, it must be trimmed to only the Force Takeover subsection.
- "who is a VEKN official" restriction (added 2026-07-13 to `og_offline` step 2 and `og_force_takeover`, gating Go-Offline/Force-Takeover to Prince/NC/IC because offline mode can mint new members): fr "qui est/sont officiel(s) VEKN", es "que es/son oficial(es) de VEKN" (with "de"), pt "que é/são oficial(is) da VEKN" (with "da"), it "che è/sono un ufficiale/ufficiali VEKN" (no preposition) — per the [[glossary]] "official" row. Listed the trio as "IC, NC ou/o Prince" (not en's "Prince, NC, or IC" order) to match each file's own pre-existing order convention for this trio (see `tournament_new_no_permission`/`err_tournament_create_forbidden`/`league_new_no_permission`, consistent across all 4 locales) — when en and the locale's established order disagree for this specific trio, follow the locale's established order, not en's.
- pt's `og_offline`/`og_force_takeover`/`og_offline_prepare` markdown bodies contain literal untranslated English button labels inside bold markers (`**Go Offline**`, `**Force Takeover**`, `**Go Back Online**`) even though the actual UI buttons are translated (`offline_go_offline`="Ficar offline", `offline_force_takeover`="Assumir controle") — a pre-existing gap, confirmed still present 2026-07-13. Don't silently fix it as a drive-by edit; it's out of scope unless asked. Worth flagging to the team as a follow-up ticket.
- `tournament_playing_latecomer` (added 2026-07-13, shown to an unregistered logged-in member viewing a mid-round tournament): confirms the `tournament_*` player-view section register per-locale — es uses **usted** here (`pregunte`, matching sibling `tournament_your_score`="Su puntuación"), fr **vous** (`vous ajouter`/`demandez`), pt **você** (`Quer jogar?`, no explicit pronoun needed), it **tu** (`aggiungerti`/`chiedi`, per the player-facing-action-UI half of the it split in [[tone_register]]). Used the "add" verb family (glossary "add" row: ajouter/añadir/adicionar/aggiungere), not the register-family verb, since the sentence is literally about the organizer performing the Add-Player action on a latecomer — same rule of thumb as the sponsor-cta precedent above. es used indirect object "añadirle" (leísmo for a person, gender-neutral) rather than "añadirlo/la" to dodge the object's unknown gender.
- "phone" (the physical device, not "mobile"/"cell"): established word per locale, confirmed 2026-07-13 across `pg_install`/`pg_calendar_feed`/`pg_offline_details`/`og_offline`/`og_force_takeover` and reused for `checkin_qr_scan_instruction` — fr **téléphone** (not portable/mobile), es **teléfono** (not móvil), it **telefono** (not cellulare), pt **celular** (not telefone/telemóvel — pt-BR convention, already noted in the top-level system prompt). "Camera" word choice follows each locale's own sibling key rather than a fixed rule: fr/es/pt use a possessive ("votre téléphone"/"tu teléfono"/"seu celular", matching how `checkin_qr_hint` says "votre caméra"/"tu cámara"/"sua câmera"), it omits the possessive and uses a bare definite article ("la fotocamera del telefono"), matching `checkin_qr_hint`'s "la fotocamera" with no "tua".
- "publish to VEKN" (the *calendar-entry* sense — the tournament has a VEKN calendar listing via the Sync-to-VEKN action — distinct from "reported/pushed to VEKN" = results submission (row above) and "registered with VEKN" = that sync action's own past participle) has its own verb+preposition. Established by `vekn_sync_rounds_required` ("Configure the number of rounds before publishing to VEKN"): fr "publier auprès de la VEKN" (config-gating register; `og_intro` narrative prose instead uses "publient sur VEKN" — both prepositions attested, pick by register), es "publicar en la VEKN", pt "publicar na VEKN", it "pubblicare sulla VEKN" — all four take the definite article with "VEKN" here (unlike the site `vekn.net`, which never does). Reused verbatim 2026-07-13 for `tfield_vekn_locked_hint`/`err_tournament_vekn_frozen_field`, rendering "already published on the VEKN calendar" as fr "déjà publié sur le calendrier VEKN", es "ya está publicado en el calendario VEKN", pt "já está publicado no calendário VEKN", it "già pubblicato nel calendario VEKN" — "VEKN calendar" itself is `og_intro`'s own section heading (fr "Calendrier VEKN", es "Calendario VEKN", pt "Calendário VEKN", it "Calendario VEKN").
- "National/Continental Championship" (tournament rank category): translated per-locale precedent lives only in `og_intro` help prose, not the actual UI — see [[glossary]] row "National/Continental Championship" and its ¶¶ footnote for the trap (the real `<select>` always shows literal English regardless of locale). fr lowercases the adjective and, in generic prose (not naming two distinct list options), the noun too ("le championnat national ou continental"); es/it capitalize both words even in generic prose ("el Campeonato Nacional o Continental" / "il Campionato Nazionale o Continentale"); pt keeps the English phrases untranslated. Used 2026-07-13 for `tfield_ranked_no_proxies_hint`/`err_tournament_rank_forbids_proxies`/`err_tournament_rank_forbids_multideck`.
- `organizers_remove_title`/`_btn`/`_self_warning`/`_add_chip` (added 2026-07-13, `OrganizerManager.svelte` confirm modal + header chip): confirmed the remove-from-role verb family (fr retirer/es eliminar/pt remover/it rimuovere) is distinct from the delete-object verb family (`common_delete` fr supprimer/pt excluir/it eliminare) — see [[glossary]] "remove vs delete" row; es doesn't split, both senses use "eliminar". Title mood follows the no-question-mark → it-imperative rule already in [[glossary]] (`players_remove_title` precedent): "Rimuovi organizzatore", not infinitive. **Register decision for `organizers_remove_self_warning`**: this is a routine self-service action (removing your own organizer role, reversible by another organizer), not the heavy offline/force-takeover admin block covered by [[tone_register]]'s it Lei/tu split — treated it as **tu**, by analogy with `profile_abandon_vekn_description` (another self-service "you're changing your own access" warning, which uses tu: "Manterrai i tuoi metodi..."). If a future string in the `organizers_*` prefix turns out to need Lei (e.g. one organizer's action affecting *another* organizer's access), don't assume this precedent carries over — re-check register per [[tone_register]]'s "check sibling keys" rule. "Co-organizer" and "organizer access" terms pulled from the pre-existing `og_co_organizers` help section rather than coined fresh — see [[glossary]] rows.

- Confirm-modal **title** mood (added to, not new as of 2026-07-13 `timer_reset_confirm_title`): titles that end in "?" use **infinitive + "?"** in fr/es/pt/it regardless of file-wide register — confirmed across `finish_confirm_title`/`reopen_confirm_title`/`decks_delete_confirm_title`/`tournament_banner_remove_confirm_title`/`league_delete_confirm` and now `timer_reset_confirm_title` ("Réinitialiser l'horloge de ronde ?" / "¿Reiniciar el reloj de ronda?" / "Reiniciar o relógio da rodada?" / "Reimpostare l'orologio del round?"). This is distinct from the no-question-mark titles noted below (`players_remove_title`), which use it-imperative instead of infinitive.
- "There is no undo" / "this action cannot be undone" canonical short-form (added 2026-07-13 for `timer_reset_confirm_body`, reusing a pre-existing dominant pattern already seen 4× each in `tournament_delete_msg`/`member_delete_warning`/`offline_force_unlock_msg`/`league_delete_confirm`): es "Esta acción no se puede deshacer.", pt "Esta ação não pode ser desfeita.", it "Questa azione non può essere annullata.", fr has two attested variants — "Cette action ne peut pas être annulée." (`tournament_delete_msg`, 1×) vs "Cette action est irréversible." (`member_delete_warning`/`offline_force_unlock_msg`/`league_delete_confirm`, 3×, dominant) — used the dominant "irréversible" form. Reuse these exact sentences verbatim for any future destructive-action confirm body rather than re-coining.
- Timer/round-clock terms (added 2026-07-13, `timer_reset_confirm_*`/`timer_add_table_time`): "clock" = horloge(fr)/reloj(es)/relógio(pt)/orologio(it), matching the existing `timer_*`/"clock stop" glossary row. "table" (physical game table, not the registration desk) = table(fr)/mesa(es)/mesa(pt)/tavolo(it). "extra" (time) stays the bare loanword "extra" in es/pt/it (per pre-existing `timer_extra_time`) but is translated "supplémentaire" in fr (also pre-existing). Register for `timer_add_table_time` (a judge-facing tooltip on per-table time-extension chips, title attribute on a `Button`, not the chip's own visible label): treated as a routine organizer/judge action (not the heavy offline/admin Lei block) and applied the **button-label mood convention** ([[tone_register]]: fr/es/pt infinitive, it tu-imperative) since it's a short standalone action phrase in a `title=` attribute like `timer_start`/`timer_pause`/`timer_reset` siblings, not descriptive running prose — fr "Donner du temps supplémentaire à cette table", es "Dar tiempo extra a esta mesa", pt "Dar tempo extra a esta mesa", it "Dai tempo extra a questo tavolo" (tu-imperative of dare).
- `rooms_empty_state`/`rooms_validation_hint`/`rooms_move_up`/`rooms_move_down`/`rooms_remove` (added 2026-07-13, `TableRoomsEditor.svelte`, organizer venue-room config for table seating): "seating" here (the overall chart, not individual seat positions — cf. `rounds_print_seating` not `rounds_alter_seating`) uses the "disposición"(es)/"disposição"(pt)/"disposizione"(it)/"placement"(fr) rendering, per [[glossary]]'s seating row. `rooms_remove` follows the remove-from-list verb family (retirer/eliminar/remover/rimuovere), not the delete-object family — a room list is a config list like the "remove organizer" pattern (`organizers_remove_*`), not a permanent asset like a banner; also matches the pre-existing `og_round_start_details` guide text "Aggiungi / Rimuovi Tavolo". `rooms_move_up`/`rooms_move_down` (icon-button aria-labels, no prior reorder-button precedent existed anywhere in the file) coined as fr "Monter"/"Descendre", es "Subir"/"Bajar", pt "Mover para cima"/"Mover para baixo", it "Sposta su"/"Sposta giù" — reuse these for any future list-reorder controls rather than re-coining. `rooms_empty_state` is a direct-address question+imperative; treated as formal vous/usted/você (file-wide organizer-UI default) but **tu** for it, since the immediate sibling `rooms_add`="Aggiungi sala" is already tu-imperative (not Lei "Aggiunga") — this rooms_* section is routine organizer config, not the heavy offline/admin Lei block.
- "pool" (the waiting group of checked-in-but-unseated players, distinct from the VTES resource Pool) is kept as a bare English loanword in all four locales — confirmed pre-existing precedent `err_tournament_raffle_no_players` (fr "dans le pool", es/pt/it "en el pool"/"no pool"/"nel pool") and reused 2026-07-13 for `rounds_alter_pool_hint`. Don't translate it (e.g. not "file d'attente"/"lista de espera").
- Undo/move vocabulary for draft seating changes (added 2026-07-13, `rounds_seating_undo`/`rounds_seating_undone`/`rounds_alter_pool_hint`): "undo" button verb reuses the pre-existing `raffle_undo` precedent — fr "Annuler", es "Deshacer", pt "Desfazer" (all bare infinitives, per the button-label-mood convention), it "Annulla" (tu-imperative). "move" as a noun (one seating change, not the verb) reuses fr "déplacement"/it "spostamento" already attested as nouns in `rounds_seating_moving` ("Déplacement de {name}"/"Spostamento di {name}"); es/pt had no prior noun-form precedent so coined "movimiento"/"movimento" by analogy with their own verb families (mover→movimiento/movimento). Past-participle SR-announcement pattern for `rounds_seating_undone` follows the existing `rounds_seating_swapped`/`rounds_seating_moved` "noun + past participle" shape: fr "Déplacement annulé", es "Movimiento deshecho", it "Spostamento annullato", pt "Movimento desfeito".
- "live round" (a round with status In Progress, as opposed to finished/not started — `err_tournament_round_not_live`, added 2026-07-13): no prior "live" precedent existed, so reused each locale's own `rounds_in_progress` status-label adjective rather than coining a new one (same-term-same-translation rule) — fr "en cours", es "en progreso", it "in corso", pt "em andamento". E.g. it "un round in corso", es "una ronda en progreso".
- **Pre-existing gap noticed, not fixed** (out of scope 2026-07-13): `rooms_title` (the feature's own section header) is inconsistent with how the feature is named in the `og_round_start_details` help-guide paragraph that references it — fr `rooms_title`="Salles" vs guide "Salles de tables"; es "Salas" vs guide "Salas de mesas"; it "Sale" vs guide "Sale Tavoli"; pt "Salas" and the guide paragraph actually leaves the feature name **untranslated as "Table Rooms"** (matching the earlier-noted pt gap pattern of leaving bold UI-label references in English). Worth a follow-up ticket; didn't touch since only 5 new keys were in scope.
- `action_bar_noshow_notice` (added 2026-07-13, organizer action-bar warning listing not-yet-checked-in players before Round 1): "no-show" has **no established key anywhere in the file** (confirmed via full-file grep across all 5 locales) — used the natural per-locale term instead of coining a fixed noun: fr "absents", es/pt "ausentes", it "assenti" (all as a predicate adjective "marqué·s absent·s"/"marcados como ausentes"/"segnati come assenti", not a standalone noun — no "no-show" noun form was coined, so don't reuse one if a future string needs the noun). "checked in" status here follows the **immediate sibling** `action_bar_waiting_initial`'s native-verb rendering (fr enregistré/es registrado/pt registrados/it registrato) rather than the file-wide "check-in is a loanword in es/pt/it" default in [[tone_register]] — pt still uses the "check-in" loanword as a *verb* elsewhere but "registrados" for the *counted status*; local sibling wins per the established check-siblings-first rule. The "marked as X" pattern for an automatic status change reuses the exact verb attested in `pg_checkin_details`'s auto-drop sentence: fr "marqué comme", es/pt "marcado como", it "segnato come" (it uses segnare, not marcare — don't swap). "reinstated"/"reinstatable" reuses `rounds_restore_blocked`'s verb: fr réintégré, es reincorporarse, pt reintegrado, it reintegrato. Confirmed (via `pg_checkin_details`'s "avant la ronde 1"/"antes de la ronda 1"/"antes da rodada 1"/"prima del round 1") that a round ordinal referenced **mid-sentence** is lowercase in fr/es/pt/it even though the same word is capitalized at the start of sibling strings like `action_bar_waiting_after_round` ("Ronde {n} terminée.") — capitalization there is sentence-position, not a fixed proper-noun rule; use lowercase whenever the round number lands mid-sentence.

- **"Use [ButtonTerm] for X" pattern — no quotes** (confirmed 2026-07-13 via pre-existing `err_tournament_use_drop_out`: en "Use DropOut for players who have played" → fr "Utilisez Abandonner pour...", es "Use Retirarse para...", pt "Use Desistir para...", it "Usa Abbandona per..."): when a sentence's whole structure IS "Use/Utilisez/Usa/Use + [the sibling button's own label term, conjugated to that locale's button-label mood] + for/pour/para/per + situation", do NOT wrap the button term in quotes — this is a different, more literal case than [[tone_register]]'s "quote a sibling button label verbatim inside a hint string" rule (that rule applies when the button label is *quoted prose describing* the button, e.g. `rounds_alter_pool_hint`'s "...then use "Seat a player" on the table"). Here the term is the direct grammatical object of the imperative verb itself, exactly mirroring how en.json itself writes it unquoted. Applied 2026-07-13 to `override_usage_hint`: "Utilisez Modifier pour..."/"Use Anular para..."/"Use Substituir para..."/"Usa Sovrascrivi per..." — reusing each locale's `override_btn` term (see [[glossary]] "override, as verb/button" row). Before choosing quoted-vs-unquoted for a new sibling-button reference, check whether en.json itself quotes it — if en doesn't quote it, don't add quotes in translation either. Third confirmed instance 2026-07-13, `offline_not_locked_nudge` ("...use Go Offline to enable offline mode"): reused each locale's exact `offline_go_offline` button rendering unquoted — fr "utilisez Passer hors ligne pour...", es "use Modo sin conexión para..." (es's button label is itself the noun phrase "Modo sin conexión", not a verb — still slotted in unquoted per the pattern, even though it reads as a mild repetition against the trailing "activar el modo sin conexión"; that repetition originates from the button label choice, not a translation error), pt "use Ficar offline para...", it "usi Vai Offline per..." (Lei-imperative "usi", since this key sits in the it Lei offline-admin block per [[tone_register]]).

- **`og_deck_management` has its own doc-level "deck" word choice, independent of UI-label convention** (found 2026-07-13 editing its "Players list" paragraph): the glossary's "UI labels keep deck as a loanword" rule applies to short UI strings (`decks_*` keys), but this specific help-guide body already translates "deck"→"mazo" throughout in es (`mazo`, `mazos`, never "deck") and "deck"→"mazzo" throughout in it (`mazzo`, `mazzi`), while fr/pt keep the bare loanword "deck" in this same doc's prose. Before writing new prose inside `og_deck_management` (or extending it), grep the doc's own existing paragraphs for its established word, not the file-wide UI-label default.
- **"missing deck" adjective, prose form, differs per locale from the standalone `decks_missing` chip word** — confirmed via `og_checkin_methods`' pre-existing "missing decklist badge" sentence: fr "deck manquant" (matches chip "Manquant"), es "mazo faltante" (chip is verb-form "Falta", not "faltante" — both attested, don't unify), pt "deck ausente" (chip is "Faltando" — a *third* pt word for the same concept, both pre-existing, don't unify), it "mazzo/decklist mancante" (matches chip "Mancante"). When writing prose that needs the adjective "a missing deck", use the doc-local adjective (faltante/ausente), not the chip's own word, for es/pt.
- **"issue(s)" (deck validation problems/errors, e.g. `deck_filter_problems`)** reuses the existing `problème(s)`(fr)/`problema(s)`(es/pt)/`problema/i`(it) rendering already established by `rounds_seating_issues`/`seating_severity_*` — not a new coinage. "deck issues" = "problèmes de deck"(fr) / "problemas de mazo"(es, matching the doc's own "mazo" word) / "problemas de deck"(pt) / "problemi di mazzo"(it, matching the doc's own "mazzo" word).
- **`deck_filter_all`="Decks" chip label**: kept as the bare loanword plural per the file-wide UI-label convention (not this doc's own translated-prose convention) — fr/es/pt "Decks", it "Deck" (Italian doesn't pluralize the loanword with -s, matching `decks_my_decks`="I tuoi Deck"/`decks_player_decks`="Deck dei Giocatori"). This is a short UI chip, not doc prose, so the UI-label rule wins over the `og_deck_management`-local rule above.
- **Referencing the "Players" list/tab by name inside running prose** (e.g. "from the **Players** list", added to `og_deck_management` 2026-07-13): bold only the tab's own name, taken from `tournament_tab_players` (fr Joueurs/es Jugadores/pt Jogadores/it Giocatori) — e.g. fr "la liste **Joueurs**", es "la lista **Jugadores**". This is distinct from the generic lowercase, unbolded "player list" phrase used elsewhere in the same doc (fr "la liste des joueurs", es "la lista de jugadores", pt "a lista de jogadores", it "la lista giocatori") — only bold+capitalize when the source string itself does (i.e. when it's naming the actual tab, not describing "a list of players" generically).

- **Third-party product menu paths — verified 2026-07-13 for `tournaments_calendar_howto`** (a hint naming Google Calendar's and Apple Calendar's actual localized "subscribe by URL" menu items): confirmed via WebFetch against support.google.com and support.apple.com's own localized guide pages (not guessed) — Google Calendar "Other calendars → From URL" = fr "Autres agendas → À partir de l'URL", es "Otros calendarios → Desde una URL", pt-BR "Outras agendas → Do URL", it "Altri calendari → Da URL". Apple Calendar (Mac) "File → New Calendar Subscription" = fr "Fichier → Abonnement à un calendrier" (current macOS wording — no "Nouvel", contradicts an older/alternate localization "Nouvel abonnement" that turns up in some search snippets; the direct guide-page fetch is authoritative), es "Archivo → Nueva suscripción a calendario", pt-BR "Arquivo → Nova Assinatura de Calendário", it "File → Nuova iscrizione a calendario". Reuse verbatim for any future string referencing these menus; re-verify via WebFetch on `support.apple.com/<locale>/guide/calendar/icl1022/mac` / `support.google.com/calendar/answer/37100?hl=<locale>` if Apple/Google ever rename them again.
- "calendar app" (the generic OS-level calendar application, not a specific named app): fr "application de calendrier", es "aplicación de calendario", pt "aplicativo de calendário" — all spelled out, no loanword — but it uses the "app" loanword: "app del calendario". Established by pre-existing `pg_calendar_feed` ("votre application de calendrier"/"la aplicación de calendario de su teléfono"/"seu aplicativo de calendário"/"nell'app del calendario") and reused 2026-07-13 for `tournaments_calendar_webcal`. Don't introduce "app" as a fr/es/pt loanword even though it's common in those languages generally — this codebase's own precedent spells it out.
- Calendar-**subscribe** concept (an .ics/webcal feed subscription) uses a distinct verb family per locale from VTES "register" (see glossary § "register"): fr s'abonner/abonnement, es suscribirse/suscripción, pt assinar/assinatura, it iscriversi/iscriviti/iscrizione — confirmed via `tournaments_calendar_subscribe`+`pg_calendar_feed` and reused 2026-07-13 for `league_calendar_subscribe`("Assinar (.ics)" etc.) and `tournaments_calendar_howto`. Note it's a false-friend trap: it's "iscriviti" is shared with the VTES tournament-registration sense elsewhere in the file — same word, two different concepts, not an error.
- fr colon/semicolon spacing: this codebase uses a **regular ASCII space** (U+0020) before `:` and `;`, not the standard French typographic non-breaking space — confirmed 2026-07-13 by codepoint-inspecting `login_error_auth` ("Erreur d'authentification : {error}"). Same finding as the pre-existing guillemet-spacing note in [[tone_register]]; apply the regular-space rule to semicolons too (no prior semicolon precedent existed before `tournaments_calendar_howto`, extrapolated from the colon rule).
- "in this tournament" as a bare locative (not the registration sense "registered *in* this tournament" = à ce tournoi/en este torneo/a questo torneo/neste torneio, per `err_tournament_not_registered`): no prior locative precedent existed, so for `sanction_dq_already_active` (2026-07-13, offline mirror of a server-side reject when a judge tries to double-DQ a player) used fr "dans ce tournoi", es "en este torneo", it "in questo torneo", pt "neste torneio" — plain locative preposition, distinct from the registration-sense "à/a" collocation. Sentence pattern borrowed from the existing `sanction_sa_no_round` "This player has not played a round yet, so..." shape (Este jugador/Ce joueur/Questo giocatore/Este jogador + verb). "disqualification" here reuses `sanction_level_disqualification`'s exact term (es descalificación, fr disqualification, it squalifica, pt desqualificação), not the `sanction_level_dq` abbreviation — confirmed via the `og_timer_extensions`/`og_round_start_details` help-guide's own "Disqualification (DQ)"/"Squalifica (DQ)" gloss pattern.

- **`league_mode_hint_rtp`/`_score`/`_gp`, `league_points_rtp`/`_gp`** (added 2026-07-13, standings-mode explanations under the League new/edit mode `<select>` and as a caption over the standings table, `frontend/src/routes/leagues/`): the pre-existing `og_reference`'s "Standings Modes"/"Modos de Clasificación"/"Modos de Classificação"/"Modalità di Classifica" section (same file, describing RTP/Score/GP modes for the help guide) is a **richer precedent than the glossary table** for this cluster — reused its established terms directly: "rating points" (fr points de rating / es puntos de rating / pt **pontos de classificação**, not "de rating" — pt's own break from the loanword, confirmed *twice* now, also in `rankings_cat_constructed_desc` / it punti rating), "GW/VP/TP" spelled out in prose reuses `pg_standings`'s own gloss sentence ("Your score shows **GW** (game wins)...") rather than coining fresh: fr victoires de partie / points de victoire / points de tournoi, es victorias de partida / puntos de victoria / puntos de torneo, pt vitórias de partida / pontos de vitória / pontos de torneio, it keeps English **singular, not pluralized** — "game win, victory point, tournament point" (matches `pg_standings` it's own singular gloss even though the en source there is plural — it deliberately doesn't add -s to these loanwords). "Winner"/"finalists" reuse `tournament_winner`/`tournament_finalist` (fr Vainqueur/finalistes, es Ganador/finalistas, pt Vencedor/finalistas, it Vincitore/finalisti) — also matches `og_reference`'s own GP-mode bullet, which independently uses the same words. "season" (points/pontos/punti de/di **saison/temporada**) had no prior precedent — coined fr "points de saison", es "puntos de temporada", pt "pontos de temporada", it "**punti stagionali**" (adjective form, not the bare noun-stack "punti stagione" — Italian favors the adjective here since, unlike GW/VP/TP, there's no English abbreviation to fall back on as a loanword).
- **Numeric-run + em-dash formatting**: kept the "10/9/8…" digit run and the U+2026 ellipsis character verbatim across all locales per instruction — digits and the em dash are not translated content. fr keeps its regular-ASCII-space-before-colon rule (see the colon/semicolon note above) inside `league_mode_hint_score`/`_gp`'s "Score cumulé des rondes préliminaires :" / "par événement :".
- **Tiny column-header abbreviations** `league_points_rtp`/`league_points_gp` (10px uppercase table headers, `frontend/src/routes/leagues/[uid]/+page.svelte`): "RtP" is kept **verbatim, unchanged, in all 4 locales** — confirmed via `pg_standings`'s own "(RtP)" parenthetical, identical in fr/es/pt/it. "GP pts" was reordered to **"pts GP"** in fr/es/pt (noun-before-qualifier word order, all three Romance languages) and **"pt GP"** in it (singular "pt", no English pluralizing -s, consistent with it's general loanword-singular convention — see the GW/VP/TP note above). "GP" itself stays bare/untranslated in all four per the existing `league_standings_gp`/`league_standings_gp_opt` precedent (already confirmed identical "Grand Prix" across all locales) — this is a proper-noun mode-name usage, distinct from `pg_leagues`'/`og_reference`'s generic lowercase "a grand prix" (translated by it only, to "un gran premio", when used as a common noun for a type of tournament series — don't confuse the two senses; the standings-mode name always stays bare "Grand Prix"/"GP" including in it).
- **Found, not fixed — GP formula discrepancy (2026-07-13)**: the pre-existing `og_reference` help-guide text describes the GP mode's scoring as a *stepped/bucketed* formula ("Winner 25, 2nd–5th 15, 6th 10, down to 11th+ 3"), while the new `league_mode_hint_gp` (same feature, same file) describes a *continuously descending* formula ("Winner 25, other finalists 15, then 10/9/8… down the standings"). These don't match — either the underlying scoring formula changed and `og_reference` is now stale, or the hint's phrasing is imprecise. Out of scope for this task (only the 5 given keys were touched); flag to the team as a possible doc-vs-UI inconsistency needing reconciliation in `og_reference`.
- **`league_meta_mode_note`/`tfield_league_format_mismatch`/`league_no_tournaments_hint`/`league_add_event`/`league_add_event_none`/`league_add_event_error`** (translated 2026-07-13, the cluster flagged as "found, not touched" earlier the same day — see `frontend/src/routes/leagues/[uid]/+page.svelte` and `TournamentFields.svelte`'s league `<select>`): "link a tournament to a league" reuses the pre-existing verb from `err_tournament_league_link_forbidden`, NOT the register-family verb — fr **lier**, es/pt **vincular**, it **collegare** (confirmed distinct from `vekn_link_btn`'s own per-locale rendering, same verb family). "event" (=tournament, used interchangeably in this cluster) reuses `league_mode_hint_rtp`'s "the league's events" noun: fr événement, es/pt evento, it evento. "league-less" (a tournament not yet attached to any league — no prior precedent, coined 2026-07-13) rendered as a prepositional phrase, not a compound adjective: fr "tournoi sans ligue", es "torneo sin liga", pt "torneio sem liga", it "torneo senza campionato". `tfield_league_format_mismatch` ("requires {format}", a disabled `<option>` suffix rendered "LeagueName — requires V5") reuses the 3rd-person-singular verb from `error_action_requires_online`: fr **nécessite**, es **requiere**, pt **requer**, it **richiede** — bare verb + `{format}`, no subject pronoun (matches en's subjectless imperative-looking phrase). Register for the two direct-address strings (`league_no_tournaments_hint`, `league_add_event_none`): treated as routine organizer-page empty-state hints (same UI class as `rooms_empty_state`, not the heavy offline/admin Lei block) — fr vous, es usted, pt você, it **tu** (`i tuoi eventi`, `non organizzi`, `Puoi anche`), per the rooms_empty_state precedent above. `league_add_event` (button) follows the button-label-mood convention: fr/es/pt infinitive ("Ajouter un événement"/"Añadir evento"/"Adicionar evento"), it tu-imperative ("Aggiungi evento"). The `« Ajouter un événement »`/`«Añadir evento»`/`“Adicionar evento”`/`«Aggiungi evento»` quoting inside `league_no_tournaments_hint` follows the sibling-button-quote convention (quote the button's own translated rendering, unquoted-vs-quoted decided by whether en.json itself quotes it — here en does quote "Add event", so all 4 locales quote it too, per each locale's own quote-mark convention).
- **Found, not touched — new uncommitted en.json keys (2026-07-13, same session as the league cluster above)**: `en.json` has an unrelated pre-existing uncommitted `csv_import_*` cluster (`csv_import_title`, `_desc`, `_columns`, `_empty`, `_no_id_column`, `_preview`, `_default_paid`, `_submit`, `_error`, `_done`, `_unmatched`, `_unmatched_hint`, `_failed`, `_reason_no_vekn`, `_reason_duplicate`, `_reason_not_found` — a CSV bulk-registration-import feature, inserted after `archon_import_title`) not part of this task's assigned keys. Not translated; out of scope. Flag for its own translation pass.
- **`csv_import_*` cluster translated 2026-07-13** (the 16 keys flagged above, a CSV ticketing-export bulk-registration modal): "member has no VEKN ID/sponsor/official/register/desk" wording all reused established precedents rather than being coined fresh — see below. Since this cluster is semantically closest to the pre-existing `og_reference` "Archon Import" paragraph and `offline_go_online_summary`/`offline_go_online_msg` (both describe matching people by VEKN ID then email, and creating vs. not creating accounts), those two were the primary precedent sources, ranked above the general [[glossary]] table per the "grep the richest precedent first" rule (see the `league_mode_hint_*` entry above).
  - **"matched" verb family**: picked **associer/asociar/associare/associar** (fr/es/it/pt) — matching `offline_go_online_summary`'s exact wording ("{matched} player(s) **matched to existing members**") — over `og_reference`'s alternate renderings for the same underlying concept (it "abbinare", pt/es "corresponder"/"corresponder"). `og_reference` itself is inconsistent between the two verb families across locales; `offline_go_online_summary` was chosen as the single source of truth for the whole `csv_import_*` cluster so all 16 keys stay internally consistent (same-term-same-translation within the new cluster, even though the wider file isn't fully consistent on this verb).
  - **VEKN ID word order**: followed `og_reference`'s Archon-import paragraph order (fr/it keep **"VEKN ID"**, es/pt reverse to **"ID VEKN"**), NOT the `add_player_no_vekn_id`/sponsor-cluster's fr override ("identifiant VEKN") — because `og_reference`'s "matches players by VEKN ID first, then email" sentence is the closer semantic sibling (bulk/automated matching) than the sponsor-a-single-player flow. Rendered as "d'abord par VEKN ID, puis par email"(fr) / "por ID VEKN primero, luego por correo electrónico"(es) / "per VEKN ID prima, poi per email"(it) / "por ID VEKN primeiro, depois por e-mail"(pt) — lifted near-verbatim from `og_reference`'s own sentence per locale.
  - **"desk" → full "registration desk" phrase**: even though the en source for this cluster just says "desk"/"the desk", used the established **full** term (`bureau des inscriptions`/`mostrador de inscripciones`/`balcão de inscrições`/`banco iscrizioni`) per the `vekn_guidance_new_member` precedent (which does the same en-says-"desk"-but-locale-says-"registration desk" expansion) rather than the bare `bureau`/`mostrador`/`banco` noun from the [[glossary]] table row. Bare noun is fine standalone; once "desk" appears as *the organizer's/registration desk* concept, expand it.
  - **"row(s)" (new noun, no prior precedent)**: fila(es)/ligne(fr)/riga(it)/linha(pt), all feminine. Pluralization marker style: es/pt/fr used the parens `(s)` style (matches `deck(s)`/`carta(s)`-style precedent for simple regular +s plurals), it used the slash `riga/e` style (Italian never does simple +s, matches `giocatore/i`/`carta/e`). Note the codebase is **not** fully consistent between `(s)` and `/x` for es/pt even for simple plurals (`mesa/s` uses slash despite being simple regular) — this was a judgment call favoring `(s)`, not a hard-and-fast rule; don't be surprised by exceptions elsewhere.
  - **"ticketing" (export/platform/source), no prior precedent — coined**: fr **billetterie**, es **venta de entradas** (spelled out, no loanword), it **biglietteria**, pt **bilheteria**. Reuse verbatim for any future ticketing-integration string.
  - **"Bulk-register"/"in bulk"**: reused `og_payment_tip`'s "bulk-set" precedent — fr **en masse**, es **en bloque**, it **in blocco**, pt **em massa**.
  - **`csv_import_error`** reused `archon_import_error`'s translations **verbatim** since the en string is character-for-character identical ("Import failed"): fr "Échec de l'importation", es "Error en la importación", it "Importazione non riuscita", pt "Falha na importação".
  - Register: `csv_import_desc`/`csv_import_submit`/`csv_import_default_paid` opening verbs matched `archon_import_description`'s sibling-title register (es/pt infinitive-as-imperative "Inscriba"/"Inscreva", it tu "Iscrivi", fr imperative "Inscrivez" vous-form) since it's the direct structural sibling (another bulk-import modal description). `csv_import_reason_no_vekn` sidestepped register entirely by using an infinitive/non-finite suggestion fragment (`à parrainer d'abord` etc.) rather than picking vous/usted/tu/você, since it's a compact list-item reason tag, not a full direct-address sentence.

- **"Add to calendar"** (`tournament_add_to_calendar`, added 2026-07-15, a small per-event link on the tournament detail page that downloads a single-event `.ics` file — distinct from the `tournaments_calendar_*`/`pg_calendar_feed` webcal **subscribe**-feed feature, which uses its own s'abonner/suscribirse/assinar/iscriversi verb family, see above): no prior "add to calendar" precedent existed in-file, so coined by analogy with each locale's established "add" verb (glossary "add" row) + the pre-existing "calendar" noun (calendrier/calendario/calendário/calendario, confirmed via `tournaments_calendar_scope_label`/`tfield_vekn_locked_hint`) — fr "Ajouter au calendrier" (infinitive), es "Añadir al calendario" (infinitive), pt "Adicionar ao calendário" (infinitive), it "Aggiungi al calendario" (tu-imperative) — follows the button-label-mood convention ([[tone_register]]) even though it's an `<a>` link, not a `<button>`, since it's a short standalone action label styled identically to one. These also match how Google/Apple Calendar's own UI phrases the same action in each language — reuse verbatim for any future single-event "add to calendar" string.

- **`tournament_ranked_badge`/`tournament_unranked_badge`/`tournament_unranked_reason_*`/`tournament_ranked_hint`/`tournament_unranked_hint_*`** (added 2026-07-15, a tournament-detail badge for VEKN rating-eligibility, rules 3.1/3.1.6 — distinct sense of "rank" from `tfield_rank`/`err_tournament_rank_forbids_*`, which is the *tournament category* enum National/Continental Championship, see [[glossary]] row): the "ranked" adjective reuses `finish_confirm_players`/`finish_confirm_warn_vekn_ids`'s existing player-level rendering (fr classé/es clasificado/pt classificado/it classificato — same root family already attested for "a player counts toward VEKN rating") rather than the `tfield_rank` noun family (Rang/Rango/Classificação/Rank) or a fresh coinage — same-term-same-concept, different grammatical subject (tournament, masculine in all 4, vs player). Badge: fr Classé/Non classé, es Clasificado/No clasificado, pt Classificado/Não classificado, it Classificato/Non classificato.
  - **"(international) VEKN ranking" noun phrase** (no prior exact precedent — coined): built as a genitive with the definite article, mirroring `tfield_open_rounds_warning`'s own "de la VEKN"/"à la VEKN"/"alla VEKN" pattern (that key is the closest semantic sibling — same "doesn't count toward the ranking" concept) rather than the bare-postpositive-tag pattern seen elsewhere for fixed proper-noun headings (e.g. "Calendrier VEKN", "procédure de placement VEKN") — fr "classement international de la VEKN", es "ranking internacional de la VEKN", pt "ranking internacional da VEKN", it "ranking internazionale della VEKN". Preposition per locale for "counts toward X" reuses `tfield_open_rounds_warning` exactly: fr **dans**, es/pt/it **para/per**. "ranking" itself is kept as the bare English loanword in es/pt/it (matching `tfield_open_rounds_warning`/`og_open_rounds`'s own usage for this exact concept — distinct from `nav_rankings`'s page-title "Clasificaciones"/"Classificações"/"Classifiche", which is the page name, not the general "counts toward the ranking" concept); fr translates to **classement**, also matching that sibling.
  - **"final" (the finals round, singular prose sense, not the `tournament_finals_heading` section-title sense which is plural in es/pt/it)**: confirmed via `err_tournament_no_finals_in_progress` (es "ninguna final"), `pg_standings`/`og_open_rounds` running prose (pt "a final", it "la finale") that all 4 locales use **singular** "final/finale" when referring to the finals-round-as-an-event in a sentence, even though the section heading itself pluralizes in es/pt/it (Finales/Finais/Finali) — see [[glossary]] "finals" row footnote †. Used singular throughout: reason chip fr "pas de finale"/es "sin final"/pt "sem final"/it "senza finale" (parallel "without X" structure across all 4), hint sentence fr "aucune finale n'a été jouée"/es "no se jugó ninguna final"/pt "nenhuma final foi jogada"/it "non è stata giocata alcuna finale".
  - **"casual" (house format)**: reused each locale's own pre-existing `og_open_rounds` word rather than coining fresh — fr **décontracté** (translates), es **casual**, pt **casual** (both cognate loanwords, already attested in that doc's own prose "eventos casuales"/"eventos casuais"), it **casual** (bare English loanword, attested "eventi casual"). Don't use "informale"/"informal" for it/pt/es — the doc's own established word is the cognate "casual".
  - **"at least N players"**: reused the file's dominant `err_tournament_*`/`og_round_start_details`/`og_finals_toss` phrasing — fr **au moins**, es **al menos**, pt **pelo menos**, it **almeno**.
  - **"rule"/"rules" (bare parenthetical citation "(rule 3.1)")**: no prior precedent for this exact bare-parenthetical pattern (the only prior VEKN rule citation, `finals_seating_procedure_link`="Tournament Rules 3.1.3", is a document-title link kept literally untranslated in all 4 locales, a different case) — translated as an ordinary common noun since it's not a proper document title: fr **règle(s)**, es **regla(s)**, pt **regra(s)**, it **regola/regole**.
  - **Found, not fixed — `tfield_open_rounds` vs `og_cfg_open_rounds`/`og_open_rounds` term mismatch** (noticed 2026-07-15): the actual UI field label `tfield_open_rounds` reads fr "Rondes ouvertes"/it "Round Aperti", but the Config-tab label `og_cfg_open_rounds` and the help-guide heading `og_open_rounds` both use fr "Rondes libres"/it "Round liberi" for the exact same feature — two different renderings coexist in the same locale file (es/pt don't have this split: both clusters agree on "Rondas abiertas"/"Rodadas Abertas"). Followed the user's explicit instruction to match `tfield_open_rounds` (the actual field label) for the new `tournament_unranked_reason_open_rounds` chip and hint, lowercased: fr "rondes ouvertes", it "round aperti". Didn't touch the pre-existing `og_cfg_open_rounds`/`og_open_rounds` mismatch — flag as a follow-up ticket if the team wants it reconciled.

- **"registration cap" (soft, non-blocking venue-capacity limit, `tfield_max_players`/`_none`/`_desc`/`players_count_capped`/`players_cap_warning_organizer`/`tournament_cap_warning_player`, added 2026-07-15)**: deliberately coined a *different* noun family from the existing "limit" word (fr limite/es límite/pt limite/it limite, already used for the hard `tfield_round_cap`/`tfield_max_rounds_no_limit` round limit) so the two concepts don't collide in the UI — used the "ceiling" metaphor instead: fr **plafond**, es **tope**, pt **teto**, it **tetto** (all masc. nouns, no gender-agreement surprises). Built as "plafond des inscriptions"/"tope de inscripciones"/"teto de inscrições"/"tetto iscrizioni" — "inscriptions" reuses the noun form already established by the glossary's "registration/check-in desk" row (bureau **des inscriptions**/mostrador **de inscripciones**/balcão **de inscrições**/banco **iscrizioni**), note it alone drops the preposition entirely in that precedent (bare noun-stacking), which is why `tfield_max_players` is "Tetto iscrizioni" not "Tetto di iscrizioni" — but full-sentence prose (`tournament_cap_warning_player`) adds the preposition back ("tetto di iscrizioni") since bare stacking only reads naturally in a short label, not running prose; same fr/es/pt pattern (label uses bare/partitive "plafond des inscriptions", sentence uses possessive "son plafond d'inscriptions"). "No cap" mirrors the pre-existing `tfield_max_rounds_no_limit` shape with the new noun: fr "Pas de plafond", es "Sin tope", pt "Sem teto", it "Nessun tetto".
  - **False-friend trap avoided**: "seats at the venue" (`tournament_cap_warning_player`, generic room/capacity sense) must NOT use the VTES game-table seat words (fr siège/es asiento/pt assento/it posto, see [[glossary]] "seat (position)" row) — those are reserved for the specific table-seating-assignment concept. Used generic capacity words instead: fr **places**, es **plazas**, pt **vagas**, it **posti** (it's "posti" is genuinely ambiguous-looking with the glossary's "posto" but is the dominant, unavoidable Italian word for "spots/room at a venue" — kept, context disambiguates).
  - Self-registration verb confirmed via `tournament_register_btn` (the actual player self-register button): fr **s'inscrire**, es **registrarse**, pt **registrar(-se)**, it **registrarsi** — this is the "register (self, reflexive)" glossary row, distinct from the organizer-registers-someone family (inscrire/inscribir/inscrever/iscrivere). Used for "You can still register" in `tournament_cap_warning_player`.
  - Generic UI "warning" (a banner/badge notice, NOT the formal VTES sanction-severity level `sanction_level_warning`/`_caution`) reuses the plain dictionary word established by `pg_deck_methods`'s deck-validation sentence, not the sanction taxonomy: fr **avertissement**, es **advertencia**, pt **aviso**, it **avvertimento**. Don't reach for the `sanction_level_*` translations here — different concept, coincidentally-overlapping English word.
  - Register: `players_cap_warning_organizer` (organizer banner) and `tournament_cap_warning_player` (player banner) both treated as routine, non-admin UI hints (not the heavy offline/admin Lei block) — es usted, fr vous, pt você, it tu, per the `rooms_empty_state`/`tournament_playing_latecomer` precedents.

- **"RtP" label unification** (2026-07-15): `tournament_col_rating`/`user_detail_col_pts`/`rankings_col_points` were relabeled from three different words (fr Classement/pt+it Rating/es Calificación, Pts, Points/Puntos/Punti) to literally **"RtP"** in all 5 locales, matching the pre-existing `league_points_rtp` abbreviation — the player-guide-taught rating-points shorthand. If any *other* stray "rating"-column label surfaces later (grep for `col_rating`/`col_pts`/`col_points`-shaped keys), unify to bare "RtP" too rather than translating.
- **`score_legend_rtp`/`_rtp_link`/`_summary_rtp`** (`ScoreLegend.svelte`, a tap-to-reveal GW/VP/TP/RtP legend, added 2026-07-15): reused `pg_standings`'s existing "your overall rating is based on your best 8 tournaments in the trailing 18 months" sentence as the precedent for the **whole** best-8/18-months clause, not just re-deriving it from the [[glossary]] "best N tournaments" row — fr "est basé sur vos 8 meilleurs tournois", es "se basa en sus mejores 8 torneos", pt "se baseia nos seus melhores 8 torneios", it "si basa sui tuoi migliori 8 tornei" (verbatim reuse of that clause's verb+object, only the time window shortened from "in the last/trailing 18 months" to a bare "sur 18 mois"/"en 18 meses"/"em 18 meses"/"in 18 mesi" since en itself dropped "last/trailing" here for a compact tooltip). "Rank" (your position, as opposed to "RtP" the raw points) reuses the [[glossary]] "official"-adjacent "ranking" split already documented for `tournament_ranked_hint`: fr translates to **classement**, es/pt/it keep **ranking** as a bare loanword (`su ranking`/`seu ranking`/`il tuo ranking`) — this is the "count toward the ranking" sense, not the `nav_rankings` page-title sense (Clasificaciones/Classificações/Classifiche), so don't swap in the page-title word here. `score_legend_rtp_link`="See rankings" (an `<a href="/rankings">`, styled like a button) DOES use the page-title word since it's literally naming/linking that page — fr "Voir les classements", es "Ver clasificaciones", pt "Ver classificações", it "Vedi classifiche" (button-label-mood convention: fr/es/pt infinitive, it tu-imperative "Vedi").
- **`share_text_deck_heading`="{name}'s deck:"** (2026-07-15, heading line in the copied share-results text, `social-text.ts`): reused the pre-existing `decks_player_deck`/`deck_upload_attr_player` ("{name}'s Deck") translations verbatim rather than re-deriving the possessive — fr "Deck de {name}", es/pt "Deck de {name}", it "Deck di {name}" — just appended a colon (fr gets a **space before the colon** per the regular-ASCII-space rule: "Deck de {name} :"; es/pt/it get no space: "Deck de/di {name}:"). "deck" stays the bare loanword (UI-label convention), not "mazo"/"mazzo" (that's the `og_deck_management`-doc-local convention only, doesn't apply to this short heading).
- **`profile_hof_member`="Hall of Fame — {wins} wins"** (2026-07-15, profile badge shown when a player has 5+ tournament wins): reused `hof_page_title`'s own per-locale rendering of "Hall of Fame" verbatim — fr/es/it keep the bare English phrase untranslated, **pt translates to "Hall da Fama"** (pt is the outlier here, confirmed via its own `hof_page_title`) — and `hof_col_wins` for "wins" (fr Victoires/es Victorias/pt Vitórias/it Vittorie). Don't retranslate "Hall of Fame" fresh per string; always pull from `hof_page_title`.
- **`user_detail_expires`="until {date}"** (2026-07-15, tiny lowercase hint after a tournament's date in the ratings table, appended with a "·" already in the template — `PlayerRatings.svelte`): fr "jusqu'à {date}", es "hasta {date}", pt "até {date}", it "fino a {date}" — plain locative "until", no prior precedent existed. **Found, not fixed**: the date passed in (`expiryMonth()` in `PlayerRatings.svelte`) is formatted via `d.toLocaleDateString(undefined, ...)` — hardcoded `undefined` locale, NOT `getLocale()` like the sibling formatter in `social-text.ts` uses — so the month name inside `{date}` won't actually follow the app's interface language. This is a frontend code bug, not a translation-file issue; out of scope for a messages-only task, flag to the team.

- **`overview_stagger_guide_link` translated 2026-07-15** (was flagged as a gap earlier the same day, now filled): the string is "[translated question] — Judges' Guide", rendered as the clickable link label itself (`ActionBar.svelte`, next to `overview_stagger_info` in the Waiting-state stagger notice). Kept **"Judges' Guide" untranslated verbatim in all 4 locales** — confirmed via `help-docs.ts`, where all four *reference-doc* titles (`rules`="VTES Comprehensive Rules", `tournament-rules`="Tournament Rules", `judges-guide`="Judges Guide", `code-of-ethics`="Code of Ethics") are hardcoded English strings, never run through `m.*()`, unlike the two *user-guide* titles (player-guide/organizer-guide) which do use `m.help_*_guide_title()`. This matches the pre-existing `finals_seating_procedure_link`="Tournament Rules 3.1.3" precedent (also kept literally untranslated across all locales). Rule: when new copy names/links one of the four raw reference docs by title, keep that title in English in every locale — only translate surrounding prose. Only the question half was translated: fr "Quand un événement devient-il non sanctionné ?", es "¿Cuándo deja de estar sancionado un evento?" (idiomatic "deja de estar" = ceases to be, not literal "se vuelve no sancionado"), pt "Quando um evento deixa de ser sancionado?", it "Quando un evento diventa non sanzionato?" — all built on each locale's already-established "sanctioned" adjective (fr sanctionné/es sancionado/pt sancionado/it sanzionato, confirmed via `profile_sponsorship_banner`/`og_open_rounds`), negated, not a fresh word.
- **`player_sitting_out` expanded 2026-07-15** to add a reassurance clause for staggered-seating events (6/7/11 players), matching the tone/vocab of its organizer-side sibling `overview_stagger_info`: the retained opening/closing sentences were left untouched per-locale, only a middle "this is normal: with this player count, sit-outs rotate so everyone plays the same number of rounds" clause was inserted, reusing each locale's own `overview_stagger_info` vocabulary for "sit-outs rotate" (fr "les joueurs assis alternent", es "los descansos rotan", pt "as pausas alternam", it "le pause ruotano") and "with this player count" (fr "avec ce nombre de joueurs", es "con este número de jugadores", pt "com esse número de jogadores", it "con questo numero di giocatori") rather than coining fresh phrasing. Register/pronoun of the pre-existing sentences was left as-is (es/it already tú/tu here, not the file's usual usted/Lei — a player-facing status message, not admin copy).
- **`tfield_section_*`/`tournament_new_missing_fields`/`venue_pick_country_hint`/`config_saved` cluster translated 2026-07-15** (the create/config-form legibility rework flagged as "found, not fixed" earlier the same day — the Svelte syntax error blocking it was fixed upstream, `svelte-check` now clean, so the cluster was completed):
  - **Section headings** (`tfield_section_basics/_location/_rounds/_visibility/_advanced`, no prior precedent for a "Basics"/"Advanced" form-section heading existed): coined short single-word headings matching common settings-UI convention rather than full phrases — fr Général/Localisation/Rondes/Visibilité/Avancé, es General/Ubicación/Rondas/Visibilidad/Avanzado, pt Geral/Localização/Rodadas/Visibilidade/Avançado, it Generale/Luogo/Round/Visibilità/Avanzate. `_rounds` reused `tournament_tab_rounds` verbatim per instruction. `_visibility` reused the bare noun already inside `tfield_standings_visibility`/`tfield_decklists_visibility`. **`_location` deliberately avoids each locale's own `tfield_venue` word** (fr/es/it "Lieu"/"Sede"/"Sede") since Location is the section heading sitting directly above the Venue field — using the same word would read as a duplicate label. Picked a distinct broader term instead: fr Localisation, es Ubicación (already distinct), pt Localização (already distinct), it Luogo (distinct from Sede). If a future key needs "location" as a bare synonym of venue rather than the section concept, don't reuse these — check context first.
  - **`tfield_standings_help_{private,cutoff,top10,public}`** and **`tfield_decklists_help_{winner,finalists,all}`**: pulled precedent from `pg_standings`'s own Standings/Viewing-Decklists sections rather than coining fresh (richer precedent than the glossary table, per the established rule) — "during the event" (fr pendant l'événement / es durante el evento / pt durante o evento / it durante l'evento), "the [score] needed to reach the top 5" simplified from `pg_standings`'s fuller "threshold" phrasing (dropped umbral/limite/soglia, kept just "necessary/necessária/necessario score/puntuación/pontuação/punteggio" to mirror en's plainer "needed"), decklists-become-public phrasing reused `finish_confirm_decks_winner/_finalists/_all` verbatim (fr devient/deviennent publique(s), es se hace/hacen pública(s), pt se torna/tornam pública(s), it diventa/diventano pubblica/pubbliche). fr semicolon in the cutoff/top10 sentences takes a regular space before it, per the existing colon/semicolon-spacing note below.
  - **`venue_pick_country_hint`** (a hint under the venue autocomplete, no prior "suggestions" word existed anywhere in the file): coined fr suggestions/es sugerencias/pt sugestões/it suggerimenti (plain cognates), paired with each locale's own `tfield_venue` word (fr lieu/es sede/pt local/it sede) — e.g. fr "voir des suggestions de lieu", pt "ver sugestões de local". Verb mood matches each locale's own `tfield_select_country` imperative (fr vous/es usted/it tu-imperative "Sélectionnez/Seleccione/Seleziona"; pt has no prior conjugated form since its own key is an infinitive-style label, so conjugated fresh as "Selecione" — você-imperative, consistent with file-wide pt register).
  - **`config_saved`="All changes saved"** (autosave confirmation chip, right after `config_saving`): coined using each locale's own save-verb past participle from `config_saving` (fr enregistrer/es guardar/pt salvar/it salvare) — fr "Toutes les modifications enregistrées" (modifications, not changements), es "Todos los cambios guardados", pt "Todas as alterações salvas", it "Tutte le modifiche salvate". Reuse these exact nouns (modifications/cambios/alterações/modifiche) for any future "changes" string in this settings-autosave context.
  - **`tournament_new_missing_fields`="Still needed: {fields}"**: no prior "still needed" precedent — coined fr "Encore requis : {fields}" (space before colon, fr convention), es "Aún se requiere: {fields}", pt "Ainda necessário: {fields}", it "Ancora necessario: {fields}". `{fields}` is populated by `missingFields.join(", ")` in `tournaments/new/+page.svelte` — the ", " separator is hardcoded locale-agnostic (a pre-existing minor gap, not fixed, out of scope for a messages-only task).
  - **`tfield_open_rounds_desc` rewritten** (teachable first-time-organizer version, replacing a terse one-liner): built from `og_open_rounds`'s own vocabulary rather than coining fresh — "house format" (fr format maison/es formato casero/pt formato caseiro/it formato casalingo), "casual" (fr décontracté/es casual/pt casual/it casual — all match that doc's own adjective), "multi-week" (fr multi-semaines/es de varias semanas/pt de várias semanas/it multi-settimana), and the NOTE-box's "never sent to VEKN / don't count toward rankings" clause (fr jamais envoyés à la VEKN.../es nunca se envían a la VEKN.../pt nunca são enviados à VEKN.../it non vengono mai inviati alla VEKN...) — note this drops "player" before "rankings" to match the new en text's plainer "rankings" (not "player rankings"). "drop-in play" had no precedent (the doc's closing paragraph only has a *verb* form, "players drop in across sessions") — coined a noun-phrase rendering instead: fr "à participation ponctuelle", es "de participación puntual", pt "de participação pontual", it "a partecipazione occasionale". fr/it use their **own field-label term "rondes ouvertes"/"round aperti"** (not the mismatched `og_open_rounds` heading term "rondes libres"/"round liberi"), per the pre-existing term-mismatch note below — this key sits directly under the `tfield_open_rounds` field, so it must match that field's own label, not the help-guide heading.

See also: [[glossary]], [[tone_register]], [[infrastructure]].
