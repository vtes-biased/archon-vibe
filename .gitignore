# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
ENV/
env/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# ruff
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Database
*.db
*.sqlite

# Environment
.env
.env.local

# Frontend
package-lock.json
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/.vite/
*.local

# Rust / Engine
engine/target/
engine/pkg/

# Local dev logs
backend.log
frontend.log

# Pytest
.pytest_cache/

# FastEmbed
.fastembed_cache

# Test results and reports (Playwright)
test-results/
playwright-report/

# GeoNames raw data (processed JSON files are committed)
backend/data/geonames_raw/

# Incoming notes
INCOMING/

# Claude Code (user-specific settings and private memory)
.claude/settings.local.json
.claude/projects/
# Stray subagent memory: `memory: project` resolves `.claude/` relative to cwd,
# so an agent run from a subdir writes here instead of the tracked root .claude/.
# Anchored to depth-1 subdirs so the root .claude/ is never matched.
/*/.claude/

# Ansible
ansible/build/
ansible/.ansible_facts/
ansible/.ansible/

# Ansible vault — decrypted vault passwords, never commit (CI writes .vault_pass in
# the runner; admins decrypt secrets/<env>.vault-pass.age into the per-env files).
ansible/.vault_pass
ansible/.beta.vault_pass
ansible/.prod.vault_pass

# ansible/secrets/ — commit ONLY the public recipients list + age-encrypted password
# files; everything else (e.g. a decrypted plaintext password) stays ignored.
ansible/secrets/*
!ansible/secrets/age-recipients.txt
!ansible/secrets/*.vault-pass.age

# Role secret files — commit only the ansible-vault (.vault) variants, never plaintext.
ansible/roles/fastapi_backend/files/officials_contacts.json
ansible/roles/fastapi_backend/files/twda_github_app.pem
ansible/*.retry
ansible/galaxy_collections/

# Local prod DB dump for migration dry-runs (never commit)
archondb.dump.gz
*.dump.gz

# Personal data (scraped NC/Prince contacts) — never commit plaintext.
# Dev keeps an untracked copy here; prod gets it via ansible-vault at deploy.
backend/src/data/officials_contacts.json
