# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
ENV/
env/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# ruff
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Database
*.db
*.sqlite

# Environment
.env
.env.local

# Frontend
package-lock.json
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/.vite/
*.local

# Rust / Engine
engine/target/
engine/pkg/

# Local dev logs
backend.log
frontend.log
public-api.log

# Pytest
.pytest_cache/

# FastEmbed
.fastembed_cache

# Test results and reports (Playwright)
test-results/
playwright-report/

# GeoNames raw data (processed JSON files are committed)
backend/data/geonames_raw/

# Incoming notes
INCOMING/

# Claude Code (user-specific settings and private memory)
.claude/settings.local.json
.claude/projects/
# `memory: project` resolves `.claude/agent-memory/` relative to cwd, so an agent
# run from a package subdir would otherwise write strays there. Each subdir's
# `.claude/agent-memory` is a committed symlink back to the root one (../../.claude/
# agent-memory), so subdir-cwd writes land in the tracked root memory. Keep per-subdir
# local settings out of the tree:
/*/.claude/settings.local.json

# pyinfra deploy: downloaded release assets
deploy/build/

# Local prod DB dumps / restore staging (never commit)
archondb.dump.gz
*.dump.gz
*.dump
archon-restore/

# Personal data (scraped NC/Prince contacts) — never commit plaintext.
# Dev keeps an untracked copy here; the deploys get it from deploy/secrets/.
backend/src/data/officials_contacts.json

# Signed NDA agreement PDF — keep out of the public repo.
BCP_NDA_Agreement.pdf
