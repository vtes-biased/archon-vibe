[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "archon_engine"
version = "0.2.1"   # keep in sync with engine/Cargo.toml; bumped by hand on engine changes
requires-python = ">=3.11"

[tool.maturin]
features = ["python"]
